<% @carts.each do |item| %>
    <%= Product.find(item.product_id).name %><br>
    <%= Product.find(item.product_id).price %>円<br>
    <%= item.qty %>個<br>
    <%= link_to '削除', cart_item_path(item.id), method: :delete %><br><br>
<% end %>
<%= link_to "戻る", products_path %><br>